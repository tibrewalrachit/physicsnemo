fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
pyvista>=0.43
omegaconf>=2.3
hydra-core>=1.3
numpy
# Optional - serverless GPU backend (inference.backend: modal):
# modal>=1.5
# python-socks[asyncio]  # only needed behind an HTTP(S) proxy
